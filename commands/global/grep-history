#!/usr/bin/env bash
# Search current user shell history.
set -euo pipefail
PATTERN="${1:-}"
if [[ -z "${PATTERN}" ]]; then
    printf 'Usage: grep-history <pattern>\n'
    exit 1
fi
grep -i -- "${PATTERN}" "${HOME}/.bash_history" 2>/dev/null || true
