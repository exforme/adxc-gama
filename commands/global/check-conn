#!/usr/bin/env bash
# Simple connectivity helper placeholder.
set -euo pipefail
TARGET="${1:-localhost}"
printf 'Checking basic connectivity to %s\n' "${TARGET}"
ping -c 1 "${TARGET}" >/dev/null 2>&1 && printf 'OK\n' || printf 'FAILED\n'
