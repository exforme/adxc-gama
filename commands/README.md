# Commands Directory

The active command registry is stored under profile command directories:

```text
/opt/adxc/profiles/<PROFILE>/commands/*.cmd
```

This directory is reserved for future global command catalog mechanics.
